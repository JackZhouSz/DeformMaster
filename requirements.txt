# DeformMaster inference dependencies.
#
# Tested with Python 3.10 + PyTorch 2.4.0 + CUDA 12.1 / 12.4.
# (RTX 5090 / Blackwell users: install torch with CUDA 12.8 — see env setup.)

# ---- Core numerics / DL ----
torch==2.4.0
torchvision==0.19.0
numpy
scipy
einops
omegaconf
tqdm

# ---- Differentiable physics ----
warp-lang>=1.0
taichi

# ---- Geometry / mesh / pointcloud ----
trimesh
plyfile
open3d
pytorch3d            # may require building from source; see install.sh
kornia

# ---- Imaging / I/O ----
opencv-python
imageio[ffmpeg]
matplotlib
h5py
joblib
Pillow

# ---- Rendering / perceptual ----
lpips
gsplat               # optional fallback rasterizer; some utils import it

# ---- Demo UI ----
gradio>=4

# ---- Hugging Face weights download ----
huggingface_hub
